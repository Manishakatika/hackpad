class Solution:
    def hasMatch(self, s: str, p: str) -> bool:

        head, tail = p.split('*')

        left, rght = s.find(head), s.rfind(tail)
        if left == -1 or rght == -1: return False

        return left + len(head) <= rght
